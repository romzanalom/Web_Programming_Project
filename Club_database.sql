

CREATE TABLE Sports(
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);

CREATE TABLE Computer(
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);

CREATE TABLE Debating(
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);

CREATE TABLE Green_Theatre(
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);

CREATE TABLE Photography(
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);

CREATE TABLE Cultural(
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);


CREATE TABLE Eco_Warrior(
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);

CREATE TABLE Green_Reading(
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);

CREATE TABLE Green_Blood(
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);

CREATE TABLE Green_Marketing (
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);


CREATE TABLE Green_Business(
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);

CREATE TABLE EEE(
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);

CREATE TABLE Textile(
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);

CREATE TABLE Law (
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);



CREATE TABLE English(
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);

CREATE TABLE Social_Bonding(
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);
CREATE TABLE FTDM (
    user_id int(200) NOT NULL unique primary key,
name varchar(200) NOT NULL,
city varchar(8) NOT NULL,
email varchar(120) unique,
phone int unique,
password varchar(120) NOT NULL,
club_name varchar(120) NOT NULL,
fee int NOT NULL
);